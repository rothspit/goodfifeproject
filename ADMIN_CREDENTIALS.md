# 管理画面ログイン情報

## 🔐 デフォルト管理者アカウント

### ログイン情報
```
電話番号: 09000000000
パスワード: admin123
```

または

```
Email: admin@hitozumanomitsu.com
パスワード: admin123
```

## 📍 管理画面URL

### 本番環境
- **ログインページ**: http://210.131.222.152:3000/admin/login
- **管理画面トップ**: http://210.131.222.152:3000/admin

### ローカル環境
- **ログインページ**: http://localhost:3000/admin/login
- **管理画面トップ**: http://localhost:3000/admin

## 🚀 初回セットアップ

### 1. データベースマイグレーション実行
本番サーバーにSSH接続後、以下を実行：

```bash
cd /var/www/goodfifeproject
mysql -u hitozumano_mitu -p'Hjmitsu^90' -D hitozumano_mitu < server/migrations/create_admin_user.sql
```

### 2. 管理者アカウントの作成確認
```bash
mysql -u hitozumano_mitu -p'Hjmitsu^90' -D hitozumano_mitu -e "SELECT id, phone_number, name, email, role FROM users WHERE role = 'admin';"
```

## 📞 CTI受注機能へのアクセス

1. 上記の管理者アカウントでログイン
2. 管理画面右上の**緑色「受注」ボタン**をクリック
3. 電話番号検索で顧客情報を確認
4. 受注を作成

## 🔒 セキュリティ注意事項

⚠️ **本番環境デプロイ後は、必ずパスワードを変更してください**

### パスワード変更方法
1. 管理画面にログイン
2. プロフィール設定から変更
3. または、データベースから直接更新：

```sql
-- 新しいパスワード（例: NewSecurePass123）のハッシュを生成して更新
UPDATE users 
SET password = '<bcryptで生成したハッシュ>' 
WHERE email = 'admin@hitozumanomitsu.com';
```

## 🛠️ トラブルシューティング

### ログインできない場合

#### 1. アカウントが存在するか確認
```bash
mysql -u hitozumano_mitu -p'Hjmitsu^90' -D hitozumano_mitu -e "SELECT * FROM users WHERE phone_number = '09000000000';"
```

#### 2. roleカラムが存在するか確認
```bash
mysql -u hitozumano_mitu -p'Hjmitsu^90' -D hitozumano_mitu -e "DESCRIBE users;"
```

#### 3. 手動でアカウント作成
```sql
-- roleカラムを追加（存在しない場合）
ALTER TABLE users ADD COLUMN role ENUM('customer', 'admin', 'cast', 'staff') DEFAULT 'customer';

-- 管理者アカウントを挿入
INSERT INTO users (phone_number, password, name, email, role) 
VALUES ('09000000000', '$2b$10$rXH3sFZDVKjDLqxqVxYYgeqJqHRJ8ZP5Vr.JiYLQl3p3zKBOGGZZi', '管理者', 'admin@hitozumanomitsu.com', 'admin');
```

## 📝 追加管理者アカウントの作成

新しい管理者を追加する場合：

1. 管理画面から新規ユーザーとして登録
2. データベースでroleを変更：

```sql
UPDATE users SET role = 'admin' WHERE phone_number = '<新しい管理者の電話番号>';
```

---

**最終更新**: 2024年（現在の日付を入力）
**管理者**: 人妻の蜜 システム管理部
